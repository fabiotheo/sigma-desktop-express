/** @type {{singleQuote: boolean, trailingComma: string, tabWidth: number, semi: boolean}} */
const config = {
    trailingComma: 'es5',
    tabWidth: 4,
    semi: true,
    singleQuote: true,
}

export default config
